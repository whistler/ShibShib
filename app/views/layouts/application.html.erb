<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="google-site-verification" content="WWR8hgOcWxopwg4ev3eND3ARP15ZBYraPc3O92KMc8w" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "bootstrap" %>
    <%= stylesheet_link_tag "shibshib" %>
    <%= stylesheet_link_tag "pagination" %>
    <%= stylesheet_link_tag "bootstrap-responsive" %>

    <style type="text/css">
      html {
        background-image:url('/assets/bg.jpg');
        background-repeat:repeat;
      }
      body {
        padding-top: 70px;
        padding-bottom: 40px;
      }
      .sidebar-nav {
        padding: 9px 0;
      }
    </style>
    <%= javascript_include_tag "application" %>
    <!-- Le fav and touch icons -->
    <%= favicon_link_tag %>
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>

  <body>

  <p>
  <% if mobile_device?.nil? %>
  <% else %>
    <% if mobile_device? %>
        <%= link_to "#{t 'mobile.full_version'}", root_url(:mobile => 0), :rel => "external" %>
      <% else %>
        <%= link_to "#{t 'mobile.mobile_version'}", root_url(:mobile => 1), :rel => "external" %>
    <% end %>
  <% end %>
  </p>
  <%= render 'layouts/header' %>
  <%= render 'layouts/facebook_sdk' %>

  <div class="notes">
    <%= render 'layouts/notice' %>
  </div>

  <div class="container">
    <%= yield %>
    <%= render 'layouts/footer' %>
  </div> <!-- /container -->

  <p id="back-top"><a href="#top"><span></span><%= t 'others.back_to_top' %></a></P>

  <%= render 'layouts/google_analytics' %>
  </body>
<%= debug(params) if Rails.env.development? %>
</html>
