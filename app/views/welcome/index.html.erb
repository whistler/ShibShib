<% @posts.each do |post|%>
<div id="hidden">
	<div class="leftPosts">
		<%= image_tag(post.image.url(:large)) %>
		</div>
	<div class="rightPosts">
		<h2><%= post.title %></h2>
	</div>
	<hr> 
</div>
<% end %>
