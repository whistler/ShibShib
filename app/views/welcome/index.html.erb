
<% @posts.each do |post|%>
	<div class="leftPosts">
		Some Left
		<%= image_tag(post.image.url(:large)) %>
		</div>
	<div class="rightPosts">
		some Right
		<h2><%= post.title %></h2>
	</div>
	<div>
	<hr> 
	</div>
<% end %>
